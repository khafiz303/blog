<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/all.css">
</head>
<body>
    <%-include('partials/header.ejs')%>

    <section>
        <div class="main">
            <div class="new-blog">
                <h2> Блог</h2>
                <form action="/api/edit/blog" method="POST" enctype="multipart/form-data">
                    <input type="hidden" value="<%=blog._id%>" name="id">
                    <input type="text" value="<%=blog.title%>" placeholder="Введите заголовок блога" name="title">
                    <input type="text" value="<%=blog.subtitle%>" placeholder="Введите подзаголовок" name="subtitle">
                    <input type="text" value="anotherTitle" placeholder="Введите дополнительные заголовок" name="anotherTitle">
                    <select name="category">
                        <option value="" disabled selected hidden>Выберите категорию</option>
                        <%
                            It.forEach(item =>{
                                if(item.id == blog.category){

                                
                        %>
                            <option value="<%=item._id%>" selected><%=item.name%></option>     
                        <%      
                            }else{
                        %>   
                                <option value="<%=item._id%>" ><%=item.name%></option> 
                        <%
                                }
                            })
                        %>
                        
                      
                    </select>
                    
                    <label for="fileInput">Выберите картинку</label>
                    <input type="file" id="fileInput" name="image" style="display: none;">
            
                    <textarea placeholder="Введите описания блога" name="description"><%=blog.description%></textarea>
                    <textarea placeholder="Введите дополнительное описания блога" name="anotherDescription"><%=blog.anotherDescription%></textarea>
                    <button type="submit">Изменить</button>
                </form>
            </div>
        </div>
        
    </section>
</body>
</html>